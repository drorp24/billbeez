<script>
	
$(document).ready(function() {
		$.ajax({
	  		url: "<%= user_mailer_weekly_url %>",
	  		data: "customer_id=" + <%= @customer.id %> + "&newsletter_id=" + "<%= @newsletter.id %>" + "&section=reminders" + "&control=yes",
	  		success: function(result) {
	  			$('#sample').html(result);
	  	}});		

})	
	
</script>
<br>

<%= link_to 'New Reminder', new_customer_newsletter_reminder_path(@customer, @newsletter), class: 'btn btn-success' %>
