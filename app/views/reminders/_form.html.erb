<%= simple_form_for @reminder, url: customer_newsletter_reminder_path(@customer.id, @newsletter.id, @reminder.id), :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= f.input :text %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
