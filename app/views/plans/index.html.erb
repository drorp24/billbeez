<script>
	
$(document).ready(function() {
		$.ajax({
	  		url: "<%= user_mailer_weekly_url %>",
	  		data: "customer_id=" + <%= @customer.id %> + "&newsletter_id=" + "<%= @newsletter.id %>" + "&section=plans" + "&control=yes",
	  		success: function(result) {
	  			$('#sample').html(result);
	  	}});		

})	
	
</script>
<br>

<%= link_to 'New Plan', new_customer_newsletter_plan_path(@customer, @newsletter), class: 'btn btn-success' %>
