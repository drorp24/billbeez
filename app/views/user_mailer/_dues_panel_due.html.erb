  <tr class="bill_background">
    <td bgcolor="#FDE9C9">
    	<% if due.supplier %>
	    	<% if logo = due.supplier.logo %>
	    		<%= image_tag logo, class: 'sup_logo' %>
	    	<% else %>	
	    		<%= due.supplier.name %>
			<% end %>
		<% end %>
	</td>
    <td><%= l due.bill.issue_date, format: :no_year if due.bill.issue_date %></td>
    <td class="amount"><%= number_to_currency due.bill.amount %></td>
    <td><%= (due.bill.due_date.to_date - Date.today.to_date).to_i if due.bill.due_date %></td> 
	<td>
		<% if due.payment_url.present? %>
			<%= link_to "שלם", due.payment_url, id: "activities" %></td>
		<% end %>
	</td>
	  <% if @control %>	
	  	<td>
		  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
		              customer_newsletter_bill_path(@customer, @newsletter, due.bill),
		              :method => :delete,
		              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
		              :class => 'btn btn-mini btn-danger' %>
	  	</td>
	  	<td>
	  		<%= link_to "Edit", edit_customer_newsletter_bill_path(@customer, @newsletter, due.bill, section: params[:section]), class: 'btn btn-mini' %>
  		</td>
  		<td>
  			<%= link_to "Add Line", new_customer_newsletter_due_line_path(@customer, @newsletter, due, section: params[:section]), class: 'btn btn-mini' %>
	  <% end %>
  </tr>
  <% due.lines.each do |line| %>
  	<tr>
	  	<td><%= line.part1 %></td>
	  	<td><%= line.part2 %></td>
	  	<td><%= line.part3 %></td>
	  	<td><%= line.part4 %></td>
	  	<td><%= line.part5 %></td>
	  	<% if @control %>
	  		<td>
			  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
	          customer_newsletter_due_line_path(@customer, @newsletter, due, line),
	          :method => :delete,
	          :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
	          :class => 'btn btn-mini btn-danger' %>
	  		</td>
	  		<td>
	  			<%= link_to "Edit", edit_customer_newsletter_due_line_path(@customer, @newsletter, due, line), class: 'btn btn-mini' %>	  			
	  		</td>
	  		<td></td>
		<% end %>
  	</tr>
  <% end %>
