<div class="panel panel-default">
  <!-- Default panel contents -->
 	<table>
			<tr class="section_title panel-heading panel-primary dues-panel" style="padding: 10px 15px !important; background-color: #D2DE7D !important; background-image: linear-gradient(to bottom, #F4F7DF 0%, #D2DE7D 100%) !important;" bgcolor="#D2DE7D">
				<th width="<%= @control ? "250" : "200"%>" class="td_right" style="text-align: right">
					<%= image_tag "bell-128.png", class: "section_title_logo section_title_element", size: "48x48" %>
				</th>
				<th  width="<%= @control ? "250" : "200"%>">
 					<a data-anchor="notifications" name="notifications" class="section_title_name", style="text-decoration: none;">
  						הודעות
  					</a>
			  	</th>
			  	<th width="<%= @control ? "250" : "200"%>" class="td_left" style="text-align: left">
			  		<% if @control %> 			
						<span class="section_title_element"><%= link_to  'Add Bill Notification', new_customer_newsletter_bill_path(@customer, @newsletter, section: params[:section]), class: 'btn btn-mini' %></span>
			  		<% else %>
			  			<span class="section_title_element"><%= link_to "חזור", "#highlights_notifications", class: 'back_link' %></span>
					<% end %>
		  		</th>
	  		</tr>			  		
	</table>
  <div class="panel-body">
    <p>בילביז סרקה את חשבונך ומצאה דברים שדורשים את תשומת לבך  </p>
  </div>

  <!-- Table -->
  <table id="bills" class="data table" cellspacing="0">	
        <thead>
          <tr>
            <th>ספק</th>
            <th>תאריך</th>
            <th>סכום</th>
            <th>סכום קודם</th>
            <th>חריגים</th>
            <th>פעולות</th>
            <% if @control %>
            	<th></th>
            	<th></th>
            	<th></th>
        	<% end %>
          </tr>
        </thead>
        	<% @newsletter.notifications.includes(:bill, :supplier, :lines, :charges).order(created_at: :asc).each do |notification| %>
        		<% next unless notification.bill %>
				<%= render 'notifications_panel_charge', notification: notification %>
			<% end %>
  </table>
</div>